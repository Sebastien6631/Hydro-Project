# Add patterns of files dvc should ignore, which could improve
# the performance. Learn more at
# https://dvc.org/doc/user-guide/dvcignore

# Sorties de prédiction, réécrites à chaque exécution de predict-archive.py
# (toutes les heures en production). Les versionner dans le même répertoire que
# les données d'entrée rendrait `dvc status` sale en permanence et figerait une
# prévision arbitraire à chaque `dvc add`.
centrales/*/prevision.json
